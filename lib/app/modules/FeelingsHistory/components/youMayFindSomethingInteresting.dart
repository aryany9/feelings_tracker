import 'package:flutter/material.dart';

class YouMayFindSomethingInteresting extends StatelessWidget {
  const YouMayFindSomethingInteresting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
